[toolchain]
# TODO(S1): pin sang version cụ thể trước khi đóng Sprint 1.
# "stable" chỉ chấp nhận được trong Phase 0.
channel = "stable"
components = ["rustfmt", "clippy"]
targets = ["x86_64-pc-windows-msvc"]
profile = "minimal"
